{{define "title"}}Force shutdown received{{end}}
{{define "summary"}}<b>forceshutdown</b> from {{.Fields.from}} — shutting down with no grace period{{end}}
{{define "fields"}}
From: <code>{{.Fields.from}}</code>
{{end}}
